@isTest (SeeAllData=true)
private class jobEmailMHSAReferrals_UnitTest {
    static testmethod void test() {
        Test.startTest();

        /*System.schedule('Test MH/SA Appointments missing Referrals for Jessica', '0 45 22 * * ?', 
        new jobEmailMHSAReferrals_Scheduler());*/

        Test.stopTest();
    }
}